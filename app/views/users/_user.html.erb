<p data-user=<%= user.id %>>
  <%= user.email %>
  <% if current_user.is_blocked?(user.id) %>
    <%= link_to "Unblocked", unblock_user_path(user.id), method: :post, remote: true %>
  <% else %>
    <%= link_to "Block", block_user_path(user.id), method: :post, remote: true %>
  <% end %>
</p>
